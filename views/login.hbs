<!doctype html>
<html lang="en">

<div class="core-content core-responsive-slide">

    <!-- Preloader -->

    <!-- Header End -->

    <!-- Page feature start -->
    <section class="page-feature py-5" style="background-position: -1141px center;">
        <div class="container text-center">
            <div class="row">
                <div class="col-md-6">
                    <h2 class=" text-left">Login</h2>
                </div>
                <div class="col-md-6">
                    <div class="breadcrumb text-right">
                        <a href="/">Home</a>
                        <span>/ Login</span>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- Page feature end -->

    <!-- Login / Registration start -->
    <section class="banner login-registration">
        <div class="vector-img">
            <img src="images/vector.png" alt="">
        </div>
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <div class="content-box">
                        <h2>Login Account</h2>
                        <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit sedo<br> eiusmod tempor incididunt
                            dolore.</p>
                    </div>
                    <form action="/login" class="sl-form" method="post">
                        <div class="form-group">
                            <label>Email or Username</label>
                            <input type="text" placeholder="example@gmail.com" name="name" id="name">
                        </div>
                        <div class="form-group">
                            <label>Password</label>
                            <input type="password" placeholder="Password" name="password" id="password">
                        </div>
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input">
                            <label class="form-check-label">Remember Password</label>
                        </div>
                        <button class="btn btn-filled btn-round" type="submit" name="login" id="login"
                            onclick="return validation()"><span class="bh"></span> <span>Login</span></button>
                        <p class="notice">Don’t have an account? <a href="/Signup">Signup Now</a></p>
                    </form>
                </div>
            </div>
        </div>
    </section>
    <!-- Login / Registration end -->

    <!-- Footer strat -->

    <!-- Footer end -->

    <!-- JS -->
</div>
<script>
    function validation() {
        var name = $('#name').val();
        var password = $('#password').val();
        if (name == '') {
            $('#name').css('border-color', '#ff0000');
        }
        else {
            $('#name').css('border-color', '#f5f5f0');
        }
        if (password == '') {
            $('#password').css('border-color', '#ff0000');
        }
        else {
            $('#password').css('border-color', '#f5f5f0');
        }
        if (name != '' && password != '') {
            return true;

        }
        else {
            return false;
        }
    }
</script>